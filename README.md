# todoList
To-Do List — a minimal, responsive task manager built with HTML · CSS · JavaScript. Features: add/delete tasks, and simple local persistence (browser storage). Built for clarity and learning — easy to extend with features like categories, or sync to a backend.
